\usepackage{tikz}
\usetikzlibrary{
shapes,
graphs,
arrows.meta,
calc,
positioning,
patterns,
decorations.pathmorphing,
}

% SCALE
\tikzset{tikz scale/.style={
	scale=#1,
	every node/.style={scale=#1}
}}

% NODE
\tikzset{default node/.style={
	draw, 
	circle,
	inner sep=0mm,
	minimum size=5mm,
	very thick,
	font=\small,
	black!70,
}}

% EDGE
\tikzset{default edge/.style={
	-{Latex[length=2mm, width=2mm]},
	very thick,
}}

% LABELS
\tikzset{label/.style={
	draw=none,
	sloped,
	midway,
}}

\tikzset{label above/.style={
	label,
	above=-1mm,
}}

\tikzset{label below/.style={
	label,
	below=-1mm,
}}